[package]
name = "opbdiff"
version = "0.3.0"
edition = "2024"
rust-version = "1.85"
license = "MIT OR Apache-2.0"
description = "Semantic diff for VeriPB-extended OPB pseudo-Boolean files"
readme = "README.md"
repository = "https://github.com/ciaranm/opbdiff"
keywords = ["opb", "veripb", "pseudo-boolean", "diff", "sat"]
categories = ["command-line-utilities", "development-tools"]
publish = false

[dependencies]
anstream = "0.6"
anstyle = "1"
anyhow = "1"
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

[dev-dependencies]
assert_cmd = "2"
predicates = "3"

[profile.release]
lto = "thin"

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = { level = "warn", priority = -1 }
